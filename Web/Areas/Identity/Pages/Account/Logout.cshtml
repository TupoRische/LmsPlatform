@page
@model LogoutModel
@{
    ViewData["Title"] = "Излез от профил";
}

<header>
    <h1>@ViewData["Title"]</h1>
    @{
        if (User.Identity?.IsAuthenticated ?? false)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">Натиснете за излизане от профил</button>
            </form>
        }
        else
        {
            <p>Успешно излязохте от профила си.</p>
        }
    }
</header>
